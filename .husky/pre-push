#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Pre-push verification..."

echo "  → TypeScript type check..."
npm run typecheck

echo "  → Root unit tests..."
npx vitest run

echo "  → Template unit tests..."
cd template && npx vitest run

echo "✅ All pre-push checks passed."
