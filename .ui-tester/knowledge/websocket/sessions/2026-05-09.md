# Session: ui-test-ws-1778330624

Date: 2026-05-09
Module: websocket
Scenarios: 1 passed / 4 blocked

## Tested

- TC1 Page Load Initial State: PASSED (all 5 checks verified)
- TC2 Connect WebSocket: BLOCKED (Vite dev server lacks WS upgrade support)
- TC3 Send Echo RPC: BLOCKED (depends on TC2 connection)
- TC4 Send Ping RPC: BLOCKED (depends on TC2 connection)
- TC5 Disconnect WebSocket: BLOCKED (depends on TC2 connection)

## Findings

- WebSocket page loads correctly with all expected elements
- Initial state: status=Closed, Connect enabled, Disconnect disabled, Input disabled
- All elements have stable data-testid selectors
- WS connection fails in Vite dev mode - server returns HTTP 500 for /api/chat/ws
- Node server entry (node.ts) has proper WS upgrade handler (server.on('upgrade'))
- This is an ENVIRONMENT LIMITATION, not a page bug

## Root Cause Analysis

- Vite dev server uses @hono/vite-dev-server which only handles HTTP requests
- WebSocket requires HTTP upgrade (101 Switching Protocols) which Vite doesn't proxy
- The Node entry (entries/node.ts line 169-181) properly handles WS upgrade
- WS tests should be run against `npm run preview` (production build with Node server) or a dedicated Node server

## Updated

- Created selectors.yml with all WS page selectors
- Created patterns.yml with all interaction patterns
- Documented Vite dev server limitation
