# Session: ui-test-1778308193

Date: 2026-05-09
Module: isr
Scenarios: 5 tested / 4 passed / 1 partial

## Tested

- ISR Cache Headers: PASSED (Cloudflare edge cache working, no X-ISR headers)
- SSR Meta Tags (title, og): PASSED (unique per page, react-helmet SSR)
- Non-ISR Routes Still Work: PASSED (API JSON, health JSON, static assets)
- SPA Hydration: PASSED (React takes over, interactive, API calls made)
- ISR Generator/SSR_DATA markers: NOT FOUND (no meta generator=ISR, no window.**SSR_DATA**)

## Findings

- ISR is implemented via Cloudflare edge cache (cf-cache-status), NOT custom X-ISR headers
- cf-cache-status: MISS on first cold request → HIT on subsequent
- cache-control: public, max-age=0, must-revalidate
- NO custom X-ISR-Status header exists
- NO <meta name="generator" content="ISR" /> exists
- NO window.**SSR_DATA** exists
- SSR works: full HTML pre-rendered per page with unique titles, meta tags, and data-testids
- Root page (/) has empty #root (no SSR content), sub-pages have full SSR content
- React Helmet (data-rh="true") successfully injects page-specific meta tags at SSR time
- Routes without trailing slash return 307 redirect to trailing-slash version
- SPA hydration confirmed: after JS loads, React takes over, makes API calls, elements interactive
- /vite.svg returns Content-Type: text/html (should be image/svg+xml - minor issue)

## Updated

- Created isr/selectors.yml with all ISR route details and cache behavior
- Created isr/patterns.yml with verification flows and tips
