# Session: ui-test-isr-1778331395

Date: 2026-05-09
Module: isr
Environment: Local dev server (http://localhost:5175)
Scenarios: 5 total / 3 passed / 2 failed

## Tested

- TC1: /todos HTML contains <title> with "Todo": FAIL
  - Local dev server returns generic `<title>Biomimic App</title>` (no SSR)
  - Cloudflare deployment has SSR with page-specific titles
- TC2: /todos HTML contains og:title meta tag: PASS (with caveat)
  - Generic og:title="Biomimic App" found, but NOT page-specific
- TC3: /content HTML contains meta description: PASS (with caveat)
  - Generic description found, but NOT page-specific
- TC4: /api/todos returns JSON not HTML: PASS
  - Content-Type: application/json, body has "success" field
- TC5: React hydration works: PASS
  - Page title set to "Todo List - Biomimic App" by React Helmet
  - Input accepts text (use `type`, not `fill`)
  - Button becomes enabled when text is entered
  - Filter buttons work
  - Todo creation requires auth token

## Findings

- Local dev (Vite) does NOT do SSR - returns SPA shell HTML for all routes
- ISR/SSR is Cloudflare-specific feature, not available in local dev
- React Helmet overrides only take effect after client-side hydration
- Todo creation requires authentication - auth middleware blocks POST
- Use `type` not `fill` for React controlled inputs (fill value gets overwritten)

## Updated

- selectors.yml: added local_dev_behavior section and hydration section
- Version bumped to 2
