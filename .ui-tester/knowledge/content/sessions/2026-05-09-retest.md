# Session: retest-1778335410

Date: 2026-05-09
Module: content
Scenarios: 3 tested / 1 FAILED

## Tested

- Content list page (/content): FAILED - API returns data in wrong shape
- Content detail page (/content/content-1): PASSED - Shows "系统升级公告" with metadata
- Content filters (category/search): Cannot verify (no items to filter)

## Findings

- **CRITICAL BUG**: Content list page shows ZERO content items
  - API `/api/public/contents?limit=20` returns: `{ success: true, data: { items: [...], count: 7 } }`
  - Code does `setContents(result.data)` which sets contents to `{ items: [...], count: 7 }` (object, not array)
  - Since `contents.length` on an object is `undefined` (falsy), neither empty state nor list renders
  - **Fix**: Change line 36 from `setContents(result.data)` to `setContents(result.data.items)`
- Content detail page works with mock data from API
- No "暂无内容" text shown (good - the bug text was removed)
- API returns 7 content items: content-1, content-2, content-13, content-14, content-15, content-19, content-20

## Updated

- selectors.yml: verified stable
- patterns.yml: no changes needed
